<%- include('../inc/top3') %>

<H1>Delete type</H1>

<% if(msg.length> 0){ %>
    <p class="alert alert-info">
            <%= msg %>
    </p>
    <% } %>

        <form action="" method="post">
            
            <label class="form-label">Xác nhận xóa loại : 
                <% 
                if (name_type != null){ %>
                    <%= name_type %>
                    <%
                }else{ %>
                    <%= msg2 %>
                    <%
                }
                %>
            </label>
           
            <div class="d-grid gap-2">
                <button class="btn btn-secondary mt-4 "  type="submit">Delete</button>
            </div>

        </form>


<%- include('../inc/bottom') %>
